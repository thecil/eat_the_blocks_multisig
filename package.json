{
  "name": "eat_the_blocks_multisig",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-local": "npx hardhat test --network hardhat",
    "deploy-local": "npx hardhat deploy --network hardhat --tags Wallet",
    "deploy-goerli": "npx hardhat deploy --network goerli --tags Wallet",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "hardhat-deploy": "^0.11.18"
  }
}
